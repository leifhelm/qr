#set page(width: 5cm, height: 5cm, margin: 0cm)
#set text(font: "New Computer Modern")
#place(image("build/msg.svg"))
#align(
  center + horizon,
  box(
    stroke: 0.1mm + black,
    fill: white,
    inset: 1mm,
  )[aarch64-unknown-linux],
)
